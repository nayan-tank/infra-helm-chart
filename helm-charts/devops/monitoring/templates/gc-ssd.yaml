kind: StorageClass
apiVersion: storage.k8s.io/v1
metadata:
  name: monitor-ssd-storage-{{ .Values.NAMESPACE | replace "_" "-"}}
  namespace: {{ .Values.NAMESPACE | replace "_" "-"}}
provisioner: kubernetes.io/gce-pd
parameters:
  type: pd-ssd
allowVolumeExpansion: true
