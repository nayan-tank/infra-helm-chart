apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-config
  namespace: {{ .Values.NAMESPACE | replace "_" "-" }}
data:
  grafana.ini: |
    [server]
    root_url = "https://{{ .Values.MONITOR.BASE_URL }}/grafana"
    serve_from_sub_path = true
